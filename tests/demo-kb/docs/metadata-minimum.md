# Metadata minimum

Every record carries a title, a date range, an extent, and a scope note. A record without a scope
note is invisible to anyone who does not already know what they are looking for.

Dates that are uncertain are recorded as ranges, never as a guess with the uncertainty dropped.
Precision that is not in the source is not created by the cataloguer.
