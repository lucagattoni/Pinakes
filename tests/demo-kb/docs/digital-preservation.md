# Digital preservation

Masters are stored in three copies: one on site, one at the partner institution, and one offline.
The offline copy exists because synchronised systems replicate deletion faithfully.

Fixity is checked quarterly by checksum. A failed checksum is treated as a storage fault until
proven otherwise, and the offline copy is never used to overwrite a live copy without review.
