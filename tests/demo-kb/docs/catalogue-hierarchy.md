# Catalogue hierarchy

Description runs from the collection down: collection, series, file, item. Description at a level is
never repeated at the level below.

A collection described only at item level is unusable, because nothing tells the reader what the
collection as a whole contains.
