# Digitisation standards

Master images are captured at 600 pixels per inch, uncompressed, with a colour target in frame. The
target is what makes the file interpretable a decade later, when the camera's own profile is gone.

Access copies are derived from the master and never captured separately. Recapturing an item to
produce a smaller file doubles the handling for no gain.
