{
 "name": "timeout",
 "branch": "timeout",
 "why": "A timeout — billable-unknown. The server may have generated, so the reservation is left open rather than voided.",
 "provenance": {
  "kind": "authored",
  "why_not_recorded": "Inducing a real timeout means holding a connection open against a live service to no purpose. The billable-unknown classification this fixture drives is asserted directly against the SDK's exception hierarchy in `stubs/anthropic.pyi`."
 },
 "responses": [
  {
   "kind": "error",
   "class": "timeout"
  }
 ]
}
